// This file is used to configure Next.js 
module.exports = {
  // Disable automatic static optimization for pages that use Chakra
  // This is needed because Chakra uses React hooks and needs the full React lifecycle
  reactStrictMode: true,
};